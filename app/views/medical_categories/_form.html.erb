<%= form_for(@medical_category) do |f| %>
  <% if @medical_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medical_category.errors.count, "error") %> prohibited this medical_category from being saved:</h2>

      <ul>
      <% @medical_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="category-field">
    <%= f.label :category_name %>
    <%= f.text_field :category_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
