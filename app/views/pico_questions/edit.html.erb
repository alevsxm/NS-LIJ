<h2 class="show">Edit PICO Question</h2>

<div id="show-container">
  <%= render 'form' %>
  <span class="delete"><%= link_to 'Delete PICO Question', pico_question_path(@pico_question), method: :delete,
    data: { confirm: 'Are you sure you want to delete this PICO Question?' } %>
  </span>
</div>

<%= link_to 'Back to PICO Question', @pico_question %>
