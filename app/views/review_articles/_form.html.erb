<%= form_for(@review_article) do |f| %>
  <% if @review_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_article.errors.count, "error") %> prohibited this review_article from being saved:</h2>

      <ul>
      <% @review_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :topic %><br>
    <%= f.text_field :topic %>
  </div>
  <div class="field">
    <%= f.label :summary %><br>
    <%= f.text_area :summary %>
  </div>
  <div class="field">
    <%= f.label :article_link %><br>
    <%= f.text_area :article_link %>
  </div>
  <div class="category-field">
    <%= f.label :medical_category_id %>
    <%= f.collection_select :medical_category_id, MedicalCategory.all, :id, :category_name%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
