<h2 class="show">Edit User</h2>

<div id="show-container">
  <%= render 'form' %>
  <% if !@user.admin %>
    <span class="delete"><%= link_to 'Delete User', user_path(@user), method: :delete,
      data: { confirm: 'Are you sure you want to delete this user?' } %>
    </span>
  <% end %>
</div>

<%= link_to 'Back', users_path %>
